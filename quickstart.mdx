---
title: "Quickstart"
description: "Pon en marcha Bloc de Notas y crea tu primera nota en minutos."
---

## Requisitos previos

Antes de comenzar, asegúrate de tener instalado lo siguiente:

- **PostgreSQL 12 o superior** — el motor de base de datos que almacena tus notas.
- **Node.js 18 o superior** — el entorno de ejecución del servidor backend.
- Acceso al archivo de backup de la base de datos (`calendario Database.backup`) y al paquete del backend (`calendario-backend.rar`), incluidos en el repositorio del proyecto.

---

<Steps>

<Step title="Configurar la base de datos">

Restaura el backup de PostgreSQL para crear la base de datos `calendario` junto con la tabla `notes` y sus cinco procedimientos almacenados.

**1. Crea la base de datos de destino:**

```sql
CREATE DATABASE calendario;
```

**2. Restaura el backup desde la línea de comandos:**

```bash
pg_restore \
  --host=localhost \
  --port=5432 \
  --username=postgres \
  --dbname=calendario \
  --no-password \
  "calendario Database.backup"
```

<Tip>
Si usas una contraseña, añade la variable de entorno `PGPASSWORD` antes del comando o usa un archivo `.pgpass` para evitar que se te solicite en cada operación.
</Tip>

**3. Verifica que la tabla y los procedimientos fueron creados correctamente:**

```sql
-- Comprueba que la tabla existe
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_name = 'notes';

-- Comprueba que los procedimientos almacenados están disponibles
SELECT routine_name
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_type = 'FUNCTION'
ORDER BY routine_name;
```

Deberías ver los cinco procedimientos: `notes_agregar`, `notes_buscar`, `notes_eliminar`, `notes_listado` y `notes_modificar`.

<Note>
La tabla `notes` tiene dos campos de estado independientes:

| Campo | Valores válidos | Propósito |
|---|---|---|
| `notes_status` | `COMPLETADO` \| `INCOMPLETO` | Indica si la tarea de la nota está terminada |
| `notes_lstatus` | `ACTIVO` \| `INACTIVO` | Controla la visibilidad lógica de la nota (borrado lógico) |

Las notas nunca se eliminan físicamente — al "borrarlas" se cambia `notes_lstatus` a `INACTIVO` y dejan de aparecer en los listados.
</Note>

</Step>

<Step title="Configurar e iniciar el backend">

**1. Extrae el paquete del backend:**

```bash
unrar x calendario-backend.rar
cd calendario-backend
```

**2. Instala las dependencias:**

```bash
npm install
```

**3. Configura la conexión a la base de datos:**

Crea un archivo `.env` en la raíz del proyecto backend con las credenciales de tu instancia de PostgreSQL:

```bash
DB_HOST=localhost
DB_PORT=5432
DB_NAME=calendario
DB_USER=postgres
DB_PASSWORD=tu_contraseña
```

**4. Inicia el servidor:**

```bash
npm start
```

El backend queda escuchando en `http://localhost:3000` por defecto. Verifica que arrancó sin errores en la consola antes de continuar.

<Warning>
Asegúrate de que el servidor PostgreSQL esté en ejecución y que el usuario configurado tenga permisos `CONNECT` y `EXECUTE` sobre la base de datos `calendario` antes de arrancar el backend.
</Warning>

</Step>

<Step title="Abrir el frontend">

El frontend es un único archivo HTML. No requiere compilación ni servidor de archivos adicional.

**Abre `frontend.html` directamente en tu navegador:**

```bash
# En macOS
open frontend.html

# En Linux
xdg-open frontend.html

# En Windows
start frontend.html
```

Alternativamente, arrastra el archivo `frontend.html` a la ventana de tu navegador.

<Tip>
Si el backend no está en `http://localhost:3000`, edita la URL base de la API dentro de `frontend.html` para que apunte a la dirección correcta antes de abrirlo.
</Tip>

</Step>

<Step title="Crear tu primera nota">

Con el backend activo y el frontend abierto en el navegador, sigue estos pasos para crear tu primera nota:

1. Localiza el formulario de **Nueva nota** en la interfaz.
2. Escribe un **título** en el campo correspondiente (`notes_name`).
3. Escribe una **descripción** en el área de texto (`notes_descr`).
4. Haz clic en **Guardar** o **Agregar** para enviar el formulario.

La nota se almacena en la base de datos con los siguientes valores por defecto:

```sql
-- Equivalente interno al crear una nota
SELECT notes_agregar(
  'Mi primera nota',     -- notes_name
  'Contenido de prueba', -- notes_descr
  'INCOMPLETO',          -- notes_status (valor inicial)
  'ACTIVO'               -- notes_lstatus (visible en listados)
);
```

Una vez guardada, la nota aparece en el listado principal de la aplicación.

<Check>
Tu primera nota ha sido creada. A partir de aquí puedes editarla para marcarla como `COMPLETADO`, o eliminarla de forma lógica desde la interfaz.
</Check>

</Step>

</Steps>

## Siguientes pasos

<CardGroup cols={2}>
  <Card title="Crear notas" icon="pencil" href="/features/creating-notes">
    Aprende a gestionar notas desde la interfaz
  </Card>
  <Card title="Estados de las notas" icon="circle-check" href="/features/note-status">
    Entiende `notes_status` y `notes_lstatus`
  </Card>
  <Card title="Configuración de la base de datos" icon="database" href="/setup/database">
    Detalles avanzados sobre la estructura de la base de datos
  </Card>
  <Card title="Referencia de la API" icon="code" href="/api/notes-agregar">
    Documentación de los procedimientos almacenados
  </Card>
</CardGroup>
